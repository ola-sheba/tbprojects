# Academic Paper Insights API Terminal-Bench Task

This task asks an agent to build a FastAPI service that uploads and analyzes PDF/DOCX academic papers, including long documents above 50k tokens.

Canonical Harbor/Terminal-Bench solution path is `solution/solve.sh`. A duplicate `Solution/solve.sh` is included only to match the folder casing requested by the user.
